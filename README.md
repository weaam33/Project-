# Project-
1. Project Idea &amp; Description • A CSP problem: assign colors to map regions so no two adjacent regions share the same color. • Each region = graph node; each shared border = edge. • We build an AI agent that solves the problem using CSP algorithms and visualizes the result.
